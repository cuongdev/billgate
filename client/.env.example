# =============================================================================
# Client (Vite) – copy to client/.env và điền giá trị
# Vite chỉ load biến có prefix VITE_
# =============================================================================

# --- API backend (dùng cho auth, API fetch) ---
# Local: http://localhost:3000  hoặc để trống nếu chạy proxy cùng origin
# Production: https://your-api-domain.com
VITE_API_URL=http://localhost:3000

# --- Firebase (Web app config) – bắt buộc cho đăng nhập Google ---
#
# Cách lấy (cùng project với backend GOOGLE_CLIENT_ID):
# 1. Vào https://console.firebase.google.com → chọn project
# 2. Project Settings (bánh răng) → General → "Your apps"
# 3. Nếu chưa có Web app: Add app → Web (</>) → đăng ký, copy toàn bộ config
# 4. Điền từng giá trị bên dưới (apiKey, authDomain, projectId, ...)
#
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
VITE_FIREBASE_APP_ID=1:your-sender-id:web:your-app-id
VITE_MEASUREMENT_ID=G-XXXXXXXXXX
